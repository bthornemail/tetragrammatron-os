# Discovery / Chat Export (Archived)

This file used to contain a very large ChatGPT export (~78k lines). It has been **archived and split** into a navigable directory:

- `dev-docs/_archive/chat-exports/2025-12-20-n-sphere-vs-n-ball/README.md`

## Authority model

- **Normative specs:** `rfc/` (start at `rfc/README.md`)
- **Formal specs / proof contracts:** `proof/` (start at `proof/INDEX.md`)
- **Implementation:** `vm/` (start at `vm/README.md`)

The archived chat export is **historical / exploratory** material and does not override the RFCs.
