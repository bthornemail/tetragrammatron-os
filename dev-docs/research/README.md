# Research Notes (Non‑Normative)

This folder contains **non‑normative** exploratory notes and extracts.

- **Authoritative specs** live in `rfc/` and `proof/`.
- **Implementation** lives in `vm/`.
- Materials here may be useful for intuition, naming, or future RFC drafts, but they do not override the RFCs.

## Index

- `geometry/sphere-ball-duality.md` — boundary vs interior framing (spheres/balls), with explicit caveats.

