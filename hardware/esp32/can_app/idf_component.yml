# Note: tetragrammatron_schema is a local component, not managed via idf_component.yml
# It is added via EXTRA_COMPONENT_DIRS in CMakeLists.txt
# This file is kept for reference but the component is resolved via CMake
